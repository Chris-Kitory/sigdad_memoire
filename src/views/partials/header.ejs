<header class="entete">
  <div class="conteneur entete-barre">
    <a href="/" class="marque">
      <svg class="marque-sceau" viewBox="0 0 40 40" xmlns="http://www.w3.org/2000/svg">
        <circle cx="20" cy="20" r="18.5" fill="none" stroke="#1B3358" stroke-width="1.5"/>
        <circle cx="20" cy="20" r="14" fill="none" stroke="#B8863A" stroke-width="1"/>
        <path d="M20 10 L23 17 L30.5 17.7 L25 22.6 L26.8 30 L20 26 L13.2 30 L15 22.6 L9.5 17.7 L17 17 Z" fill="#1B3358"/>
      </svg>
      <span class="marque-texte">
        <span class="marque-titre">SIGDA</span>
        <span class="marque-sous">Commune de Makala</span>
      </span>
    </a>

    <% if (!utilisateur) { %>
      <nav class="entete-nav">
        <a href="/#suivi">Suivre une demande</a>
        <a href="/login" class="bouton bouton-secondaire bouton-petit">Se connecter</a>
        <a href="/register" class="bouton bouton-primaire bouton-petit">Créer un compte</a>
      </nav>
    <% } else { %>
      <div class="entete-utilisateur">
        <span><%= utilisateur.email %></span>
        <button type="button" class="bouton-texte" onclick="deconnexion()">Se déconnecter</button>
      </div>
    <% } %>
  </div>
</header>

<script>
  async function deconnexion() {
    await fetch('/auth/logout', { method: 'POST', credentials: 'include' });
    window.location.href = '/';
  }
</script>
